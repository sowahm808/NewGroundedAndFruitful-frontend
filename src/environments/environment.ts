export const environment = {
  production: false,
  useEmulators: true,
  firebase: { apiKey: '', authDomain: 'localhost', projectId: 'grounded-fruitful-dev', appId: '' },
  appCheckSiteKey: '',
} as const;
